package com.ltrojanowski.morph

/*
import com.ltrojanowski.morph.api.Morph
import com.ltrojanowski.morph.mocks.into

class UserRow(val name: String, val isMale: Boolean)

class AgeRow(val age: Int)

@Morph(from = [UserRow::class])
class User(val name: String, val isMale: Boolean, val age)


fun main(args: Array<String>) {

    val userRow = UserRow("foo", true)

    val user = userRow.into<User>().morph()
}*/
